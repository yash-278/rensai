// A directory is available offline only after its download finishes.
// Older downloads without this marker retain their existing behavior.
export const DOWNLOAD_INCOMPLETE_FILE = '.rensai-incomplete';
